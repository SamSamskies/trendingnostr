/** Authors hidden from the trending feed (lowercase hex pubkeys). */
export declare const HIDDEN_AUTHOR_PUBKEYS: ReadonlySet<string>;

/** NIP-56 (kind 1984) spam reporter pubkey (lowercase hex). */
export declare const SPAM_REPORTER_PUBKEY: string;
